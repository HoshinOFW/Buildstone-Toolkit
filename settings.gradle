pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {url = uri("$rootDir/libs/m2")}
    }
}

plugins {
    id "com.github.hoshinofw.multiversion.multiversion-settings" version "0.1.0"
}
rootProject.name = 'buildstonetoolkit'

